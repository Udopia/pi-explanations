# pi-explanations
Determine the Prime Implicants of Random Forest Classifiers

  * install gbd-tools (pip3)
  * download and setup gbd databases
  * install the solbert module with `python3 -m pip install ./solbert`
  * hack and run eval.py
